{
  "private": true,
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.7.0",
    "parcel": "2.7.0",
    "purescript": "^0.15.4",
    "spago": "^0.20.9"
  },
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "serve": "parcel dev/index.html --open",
    "build-prod": "rm -rf prod dist && mkdir -p prod && cp dev/* prod/ && spago build && parcel build prod/index.html --public-url ./ && cp -r static dist/static"
  },
  "dependencies": {
    "esbuild": "^0.28.0"
  }
}
